const request = require("supertest");
const { response } = require("./app");
const app = require("./app");

describe('url API', () => {
    it('GET /url_id --> show the original url', () =>{

    });

    it('GET /url_id --> 404 not found', () => {

    });

    it('POST /url --> created a new shorten_url', () => {

    });

    it('GET /url --> validates request body', () => {

    });
})